import {createTheme} from "@mui/material";

const globalTheme = createTheme({
    palette: {
        mode: 'light',
        primary: {
            main: "#EF6C00",
        },
    },
});

export default globalTheme;